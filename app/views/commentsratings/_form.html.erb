<%= form_with(model: commentsrating) do |form| %>
  <% if commentsrating.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(commentsrating.errors.count, "error") %> prohibited this commentsrating from being saved:</h2>

      <ul>
        <% commentsrating.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :no, style: "display: block" %>
    <%= form.number_field :no %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :comment_id, style: "display: block" %>
    <%= form.text_field :comment_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
